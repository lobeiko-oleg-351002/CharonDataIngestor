name: CharonDataIngestor CI

on:
  push:
    paths:
      - 'src/**'
      - '.github/workflows/ci.yml'
  pull_request:
    paths:
      - 'src/**'
      - '.github/workflows/ci.yml'

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  DOTNET_VERSION: '9.0.x'
  PROJECT_PATH: 'src'
  SOLUTION_FILE: 'CharonDataIngestor.sln'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    services:
      rabbitmq:
        image: rabbitmq:3-management-alpine
        ports:
          - 5672:5672
          - 15672:15672
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        
    - name: Restore dependencies
      working-directory: ${{ env.PROJECT_PATH }}
      run: dotnet restore ${{ env.SOLUTION_FILE }}
      
    - name: Build solution
      working-directory: ${{ env.PROJECT_PATH }}
      run: dotnet build ${{ env.SOLUTION_FILE }} --configuration Release --no-restore
      
    - name: Run unit tests
      working-directory: ${{ env.PROJECT_PATH }}
      run: dotnet test CharonDataIngestor.Tests/CharonDataIngestor.Tests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"
      continue-on-error: false
      
    - name: Run integration tests
      working-directory: ${{ env.PROJECT_PATH }}
      env:
        RABBITMQ_HOSTNAME: localhost
        RABBITMQ_PORT: 5672
        RABBITMQ_USERNAME: guest
        RABBITMQ_PASSWORD: guest
      run: dotnet test CharonDataIngestor.IntegrationTests/CharonDataIngestor.IntegrationTests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=integration-test-results.trx"
      continue-on-error: false
      
    - name: Publish test results
      uses: EnricoMi/publish-unit-test-result-action@v2
      if: always()
      with:
        files: |
          ${{ env.PROJECT_PATH }}/**/test-results.trx
          ${{ env.PROJECT_PATH }}/**/integration-test-results.trx
        check_name: 'Test Results'
        fail_on: 'test failures'
        
    - name: Publish build artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: charondataingestor-build-artifacts
        path: |
          ${{ env.PROJECT_PATH }}/**/bin/Release/**/*
          ${{ env.PROJECT_PATH }}/**/publish/**/*
        retention-days: 7

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./src
        file: ./src/Dockerfile
        push: false
        tags: charondataingestor:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

